<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="10dp">

    <!-- Üst Başlık ve Hızlı Butonlar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="6dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚡"
            android:textSize="18sp"
            android:paddingEnd="6dp" />

        <LinearLayout
            android:id="@+id/widget_header_title_box"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hızlı Notlar &amp; Görevler"
                android:textColor="#0F172A"
                android:textSize="13sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Bekleyen görevler"
                android:textColor="#64748B"
                android:textSize="10sp" />
        </LinearLayout>

        <!-- Yenile Butonu -->
        <TextView
            android:id="@+id/widget_btn_refresh"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_btn_bg"
            android:gravity="center"
            android:text="🔄"
            android:textSize="12sp"
            android:layout_marginEnd="6dp" />

        <!-- Hızlı Ekleme Butonu -->
        <TextView
            android:id="@+id/widget_btn_add"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_btn_bg"
            android:gravity="center"
            android:text="➕"
            android:textColor="#2563EB"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E2E8F0"
        android:layout_marginBottom="4dp" />

    <!-- Kaydırılabilir Görev Listesi (Microsoft To-Do / Google Görevler Tarzı) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/widget_tasks_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="#F1F5F9"
            android:dividerHeight="1dp"
            android:drawSelectorOnTop="false" />

        <!-- Boş Durum Görünümü -->
        <LinearLayout
            android:id="@+id/widget_empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone"
            android:padding="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="✨"
                android:textSize="22sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Bekleyen görev yok"
                android:textColor="#64748B"
                android:textSize="12sp"
                android:textStyle="bold"
                android:paddingTop="4dp" />

            <TextView
                android:id="@+id/widget_btn_empty_add"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="＋ Yeni Görev Ekle"
                android:textColor="#2563EB"
                android:textSize="11sp"
                android:textStyle="bold"
                android:background="@drawable/widget_btn_bg"
                android:paddingHorizontal="10dp"
                android:paddingVertical="5dp"
                android:layout_marginTop="8dp" />
        </LinearLayout>
    </FrameLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E2E8F0"
        android:layout_marginTop="4dp" />

    <!-- Alt Çubuk (Footer) -->
    <LinearLayout
        android:id="@+id/widget_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:paddingTop="4dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="TincNote • Çevrimdışı &amp; Senkronize"
            android:textColor="#94A3B8"
            android:textSize="9sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Aç ➔"
            android:textColor="#3B82F6"
            android:textSize="9sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>
